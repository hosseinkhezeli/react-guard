import { AlertProps } from '../types';
export declare function Alert({ type, message, description, closable, closeIcon, showIcon, icon, banner, onClose, action, }: AlertProps): import("react/jsx-runtime").JSX.Element | null;
